services:
  searxng:
    image: searxng/searxng:latest
    container_name: phongg-searxng
    ports:
      - "8888:8080"
    environment:
      - SEARXNG_BASE_URL=http://127.0.0.1:8888/
    restart: unless-stopped
